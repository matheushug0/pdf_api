services:
  - type: web
    name: tripmee-pdf-api
    runtime: node
    plan: free
    region: oregon
    buildCommand: "npm install"
    startCommand: "node index.js"
    envVars:
      - key: NODE_ENV
        value: production
